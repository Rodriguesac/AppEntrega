Rodrigues Entregador v1.6.0 - Push único

Esta versão junta a V1.5 com a próxima etapa:
- operação real sem botão de simulação na tela principal;
- tela de espera para pedido real;
- histórico lendo driverHistory do Firebase;
- ganhos/resumo lendo driverHistory;
- versão visual marcada como v1.6.0 operação real + histórico;
- modo teste continua escondido em Mais;
- mantém listeners de rides, drivers e FCM.

Depois de copiar/substituir no projeto:

git status
git add .
git commit -m "Operacao real v1.6 Firebase historico"
git push
